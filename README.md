# MERN App for Shortening links

## Start app
Create config file for connect mongodb database config/default.json from config/default-example.json

Install dependencies:

`npm i`

Run server:

`npm run start`

Run client app:

`npm run client`
